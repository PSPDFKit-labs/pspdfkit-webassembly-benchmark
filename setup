#!/bin/bash

rm -rf vendor
mkdir -p vendor
wget https://customers.pspdfkit.com/npm/${PSPDFKIT_DOWNLOAD_KEY}/latest.tar.gz
tar xvf latest.tar.gz

cp -R package/dist vendor/pspdfkit

rm -rf package
rm latest.tar.gz
